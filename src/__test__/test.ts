// const fn = (...args: any[]) => {
//   args.forEach(arg => console.log(arg));
// };

// const myName = 'Snicker';
// const weather = 'Rainy';

// fn('a', 'b', 'c');
// fn`hello ${myName}`;
// fn`hello ${myName}, oops`;
// fn`hello ${myName}, Today is ${weather}`;

// enum Color {
//   Red,
//   Green,
//   Blue,
// }

// console.log(Color[0]);

const big = BigInt(10);
const small = BigInt(2);
console.log(big - small);
